import React from 'react';
import { View, Text } from 'react-native';

export default function TotalDisplay({ totals }) {
  return (
    <View>
      <Text>Totals</Text>
      {/* TODO: Display totals per person */}
    </View>
  );
} 